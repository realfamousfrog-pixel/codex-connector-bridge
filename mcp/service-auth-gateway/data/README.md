# data 目录说明

本目录用于本地运行态数据，不作为版本控制中的真实状态来源。

默认不纳入 Git 的文件包括但不限于：

- `state.json`
- `sessions.json`
- `google-oauth-live.json`

原因：

- 这些文件可能包含本地认证状态、流程残留或联调痕迹
- worktree 只承载代码和文档，不承载本地登录态
- 不应把某一台机器上的运行态数据固化进仓库基线

如需演示结构或测试隔离，请使用单独的示例文件或测试目录，而不要直接提交真实运行态文件。
